[pytest]
asyncio_mode = auto
addopts = -v
testpaths = sample/tests cal/tests julee_example
markers =
    e2e: marks tests as end-to-end tests (deselect with '-m "not e2e"')
    unit: marks tests as fast unit tests with mocked dependencies
